#include <set>

set<int>name;  создание контейнера
.insert(число)  ложит в контейнер в отсартированном виде
.erase(число)   удаляет из контейнера
В контейнер нельзя вставить 2 одинаковых объекта
.count(число)	выводит количество цифр которые в скобках
если контейнер состоит из 2, 6, 9, 1, 2, 0  то name.count(2) покажет в сиауте  2
set_union(user.begin(),user.end(),user1.begin(),user1.end(),inserter(user,user.begin));  объединение 2х контейнеров
итератор множестав set нельзя переименовывать по этому к нему нужно использовать функ inserter();

