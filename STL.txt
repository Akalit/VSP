ћассивы

auto				все ипы значений как инт чар и тд определ€етьс€ автоматически

#include <vector>

vector<type(тип)>name

int arr[2] = {1,2};
vector<int>name(arr,arr+2) (почему +2 а не +1)   беблиотека построена таким образом что должно охъватыватьс€ то что стоит после последнего символа
¬ нашем случае послед символ 2 мы должны вз€ть то что зха ним
и потом вывод 0 элем и 1

инициализаци€ другим способом

vector<int>name(12,0)		состоит из двенадцати нулей

for(auto q : name)   		цыкл дл€ вывода выводит  name[q] можно писать просто q
cout<<q << endl;


__________________________________________________________________________________________________________________________________________________________________

vector<int> a;
for(int q =0 ; q < 10 ; q++)
a.push_back(q+1)  		 заполнение перем, в масс



typedef (тип) name  = мен€ет название типа данных, и его можно вызывать дл€ использовани€ (typedef vector<int>ar; <- ar == тип) ar arr; <- arr == контейнер



 